import { ModulePage } from "@/components/module-page";

export default function DocsPage() {
  return <ModulePage eyebrow="Project scope" title="Phạm vi MVP" description="Nguồn nghiệp vụ chính nằm trong thư mục docs của repository." items={["4 role kỹ thuật, 2 role đăng ký công khai", "Không có Maker/Checker", "Giấy phép thay eKYC nhiều lớp", "Admin duyệt cứu trợ và hậu kiểm"]} />;
}
