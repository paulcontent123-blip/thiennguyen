import { ModulePage } from "@/components/module-page";

export default function SosPage() {
  return <ModulePage eyebrow="Public intake" title="Bản đồ SOS" description="Skeleton cho tiếp nhận báo cáo và hàng đợi điều phối của Admin." items={["Bản đồ công khai", "Gửi báo cáo SOS", "Xác minh dữ liệu", "Admin điều phối đội đã duyệt"]} />;
}
